# Hoisted node_modules for the whole monorepo. Required by Expo/Metro so React
# Native resolves a single copy of react / react-native; the web app works fine
# under hoisted too, so one linker covers both.
node-linker=hoisted
