// Shopify Storefront query + mutation strings now live in the shared
// @nailismo/shopify package. Re-exported here so existing
// `@/lib/shopify/queries` imports keep working unchanged.
export * from "@nailismo/shopify/queries";
