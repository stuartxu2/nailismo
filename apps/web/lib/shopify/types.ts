// Shopify Storefront types now live in the shared @nailismo/shopify package
// (consumed by both web and the mobile app). Re-exported here so existing
// `@/lib/shopify/types` imports keep working unchanged.
export * from "@nailismo/shopify/types";
